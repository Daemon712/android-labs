<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ru.foobarbaz"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="17" android:targetSdkVersion="23"/>
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">
        <activity android:name=".MainActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".calculator.CalculatorActivity" android:label="Калькулятор"/>
        <activity android:name=".converter.ConverterActivity" android:label="@string/converter_label"/>

        <activity android:name=".notebook.activity.NoteListActivity" android:label="Записная книжка"/>
        <activity android:name=".notebook.activity.NoteActivity" android:label="Заметка"/>
        <activity android:name=".notebook.activity.TagListActivity" android:label="Список тегов"/>
        <activity android:name=".notebook.activity.TagActivity" android:label="Тег"/>

        <activity android:name=".paint.PaintActivity" android:label="Графический редактор"/>
    </application>
</manifest>
